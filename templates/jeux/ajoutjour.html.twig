{% extends "base.html.twig" %}

{% block body %}
<div class="conainter">
    {{form_start(formulaire)}}

    {# {{form_widget(formulaire)}} #}
    <div class="form_group">
        {{form_row(formulaire.nomJoueur,{"label":"Nom: ", "attr":{"placeholder":"donner votre nom",
        "class":"form-control"}}) }}
    </div>

    <div class="form_group">
        {{form_row(formulaire.email,{"label":"Email: ", "attr":{"placeholder":"votre email", "class":"form-control"}})}}
    </div>

    <div class="form_group">
        {{form_row(formulaire.password,{"label":"mot de passe: ", "attr":{"placeholder":"mot de passe",
        "class":"form-control"}})}}
    </div>
    <div class="form_group">
        {{form_row(formulaire.pseudonyme,{"label":"Pseudo: ","attr":{"placeholder":"", "class":"form-control"}})}}
    </div>

    <div class="form_group">
        {{form_row(formulaire.birthdate,{"label":"Date de naissance: ","attr":{}})}}
    </div>
    <div class="form_group">
        {{form_row(formulaire.avatar,{"label":"photo de profil: ","attr":{"placeholder":"", "class":"form-control"}})}}
    </div>
    {{form_row(formulaire.envoyer,{"attr":{"class":"btn btn-danger"}})}}

    {#
    <button type="submit" class="btn btn-success">Envoyer</button> #}
    {{form_end(formulaire)}}


</div>

{% endblock %}